# Build-Artefakte
.build/
.build-x86/
dist/

# Generiertes Xcode-Projekt (aus project.yml via xcodegen erzeugbar)
activities.xcodeproj/

# Zugangsdaten / Secrets (NIEMALS einchecken)
.env
/env

# macOS
.DS_Store

# Python-Altbestand
**/__pycache__/
legacy-python-cli/.pytest_cache/

# Sitzungsmitschriften des Agenten – Notizen des Eigentuemers, kein Bestandteil
# des Projekts. ⚠️ `release.sh` macht `git add -A`; ohne diese Zeile landen sie
# im naechsten Release-Commit.
session-*.md
